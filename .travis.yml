sudo: false
language: python
python:
  - "3.4"
  - "3.5"

branches:
  only:
    - master

install:
  - make dev-setup
  - env
  - if [[ "$TRAVIS_PYTHON" == "3.4" ]]; then source venv/bin/activate; pip install typing; deactivate; fi

script:
  - make test
