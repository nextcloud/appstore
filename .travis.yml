sudo: false
language: python
python:
  - "3.4"
  - "3.5"

branches:
  only:
    - master

install:
  - make dev-setup
  - if [[ "$TRAVIS_PYTHON_VERSION" == "3.4" ]]; then source venv/bin/activate; pip install typing; deactivate; fi

script:
  - make test
